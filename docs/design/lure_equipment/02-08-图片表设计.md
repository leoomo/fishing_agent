# 02-08 - 图片表设计

> **文档版本**: v1.0
> **最后更新**: 2025-11-24
> **状态**: 已确定

## 概述

本文档定义图片存储的数据库表结构，包括产品图片、钓组图解、鱼类图片的关联管理。

---

## 表结构设计

### 1. product_images（产品图片关联表）

**用途**：管理所有图片与实体（产品/钓组/鱼类）的关联关系

```sql
CREATE TABLE product_images (
    -- 主键
    image_id INTEGER PRIMARY KEY AUTOINCREMENT,

    -- 关联实体（三选一，其他为NULL）
    equipment_id INTEGER,                   -- 关联装备
    rig_type_id INTEGER,                    -- 关联钓组类型
    fish_species_id INTEGER,                -- 关联鱼类

    -- 图片信息
    image_url TEXT NOT NULL,                -- 图片路径/URL
    image_type TEXT NOT NULL,               -- 图片类型
    description TEXT,                       -- 图片描述（用于搜索和Alt文本）
    display_order INTEGER DEFAULT 0,        -- 展示顺序

    -- 向量化相关
    embedding_id TEXT,                      -- Chroma向量ID（用于以图搜图）
    is_vectorized INTEGER DEFAULT 0,        -- 是否已向量化

    -- 元数据
    file_size INTEGER,                      -- 文件大小（字节）
    width INTEGER,                          -- 图片宽度
    height INTEGER,                         -- 图片高度
    format TEXT,                            -- 图片格式（jpg/png/webp）

    -- 审计字段
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

    -- 外键约束
    FOREIGN KEY (equipment_id) REFERENCES equipment(equipment_id) ON DELETE CASCADE,
    FOREIGN KEY (rig_type_id) REFERENCES rig_types(id) ON DELETE CASCADE,
    FOREIGN KEY (fish_species_id) REFERENCES fish_species(id) ON DELETE CASCADE,

    -- 约束：必须关联一个实体
    CHECK (
        (equipment_id IS NOT NULL AND rig_type_id IS NULL AND fish_species_id IS NULL) OR
        (equipment_id IS NULL AND rig_type_id IS NOT NULL AND fish_species_id IS NULL) OR
        (equipment_id IS NULL AND rig_type_id IS NULL AND fish_species_id IS NOT NULL)
    )
);

-- 索引
CREATE INDEX idx_images_equipment ON product_images(equipment_id);
CREATE INDEX idx_images_rig ON product_images(rig_type_id);
CREATE INDEX idx_images_fish ON product_images(fish_species_id);
CREATE INDEX idx_images_type ON product_images(image_type);
CREATE INDEX idx_images_vectorized ON product_images(is_vectorized);
```

---

### 2. image_type 枚举值

| image_type | 说明 | 使用场景 |
|------------|------|---------|
| `main` | 主图 | 产品列表展示 |
| `detail` | 细节图 | 产品详情页 |
| `infographic` | 图解/示意图 | 钓组组装说明 |
| `action` | 动作图/泳姿图 | 拟饵动作演示 |
| `color` | 涂装/配色图 | 拟饵配色展示 |
| `size` | 尺寸对比图 | 规格对比 |
| `habitat` | 生态环境图 | 鱼类栖息地 |
| `anatomy` | 特征/解剖图 | 鱼类识别特征 |

---

## 文件存储结构

### 目录结构

```
src/tools/lure/data/images/
├── products/                    # 产品图片
│   ├── rods/                   # 鱼竿
│   │   ├── shimano_zodias_264ml_main.jpg
│   │   └── shimano_zodias_264ml_detail_01.jpg
│   ├── reels/                  # 渔轮
│   ├── lines/                  # 鱼线
│   └── lures/                  # 拟饵
│       ├── megabass_vision110_main.jpg
│       ├── megabass_vision110_color_mat_tiger.jpg
│       └── megabass_vision110_action.gif
├── rigs/                        # 钓组图解
│   ├── texas_rig_assembly.png
│   ├── texas_rig_components.png
│   ├── carolina_rig_assembly.png
│   └── dropshot_rig_assembly.png
└── fish/                        # 鱼类图片
    ├── largemouth_bass_main.jpg
    ├── largemouth_bass_habitat.jpg
    └── mandarin_fish_main.jpg
```

### 文件命名规范

```
{entity_name}_{image_type}[_{variant}].{format}

示例:
- shimano_zodias_264ml_main.jpg        # 产品主图
- shimano_zodias_264ml_detail_01.jpg   # 细节图（带序号）
- megabass_vision110_color_mat_tiger.jpg  # 特定涂装
- texas_rig_assembly.png               # 钓组组装图
- largemouth_bass_main.jpg             # 鱼类主图
```

---

## ImageStorageAdapter 设计

### 接口定义

```python
from abc import ABC, abstractmethod
from pathlib import Path
from typing import Optional, BinaryIO
from dataclasses import dataclass


@dataclass
class ImageMetadata:
    """图片元数据"""
    file_size: int
    width: int
    height: int
    format: str


class ImageStorageAdapter(ABC):
    """图片存储适配器抽象基类"""

    @abstractmethod
    def save(self, image_data: bytes, relative_path: str) -> str:
        """保存图片，返回访问URL"""
        pass

    @abstractmethod
    def delete(self, relative_path: str) -> bool:
        """删除图片"""
        pass

    @abstractmethod
    def get_url(self, relative_path: str) -> str:
        """获取图片访问URL"""
        pass

    @abstractmethod
    def exists(self, relative_path: str) -> bool:
        """检查图片是否存在"""
        pass

    @abstractmethod
    def get_metadata(self, relative_path: str) -> Optional[ImageMetadata]:
        """获取图片元数据"""
        pass
```

### 本地存储实现

```python
import os
from PIL import Image
import io


class LocalImageStorage(ImageStorageAdapter):
    """本地文件系统存储"""

    def __init__(self, base_path: str):
        self.base_path = Path(base_path)
        self.base_path.mkdir(parents=True, exist_ok=True)

    def save(self, image_data: bytes, relative_path: str) -> str:
        full_path = self.base_path / relative_path
        full_path.parent.mkdir(parents=True, exist_ok=True)

        with open(full_path, 'wb') as f:
            f.write(image_data)

        return self.get_url(relative_path)

    def delete(self, relative_path: str) -> bool:
        full_path = self.base_path / relative_path
        if full_path.exists():
            full_path.unlink()
            return True
        return False

    def get_url(self, relative_path: str) -> str:
        # 本地路径转为file://协议或相对路径
        return f"file://{self.base_path / relative_path}"

    def exists(self, relative_path: str) -> bool:
        return (self.base_path / relative_path).exists()

    def get_metadata(self, relative_path: str) -> Optional[ImageMetadata]:
        full_path = self.base_path / relative_path
        if not full_path.exists():
            return None

        file_size = full_path.stat().st_size
        with Image.open(full_path) as img:
            width, height = img.size
            format = img.format.lower() if img.format else 'unknown'

        return ImageMetadata(
            file_size=file_size,
            width=width,
            height=height,
            format=format
        )
```

### OSS存储实现（预留）

```python
class OSSImageStorage(ImageStorageAdapter):
    """阿里云OSS存储（预留实现）"""

    def __init__(self, endpoint: str, bucket: str, access_key: str, secret_key: str):
        self.endpoint = endpoint
        self.bucket = bucket
        # self.client = oss2.Bucket(...)  # 未来实现

    def save(self, image_data: bytes, relative_path: str) -> str:
        # TODO: 实现OSS上传
        raise NotImplementedError("OSS storage not implemented yet")

    def get_url(self, relative_path: str) -> str:
        return f"https://{self.bucket}.{self.endpoint}/{relative_path}"

    # ... 其他方法
```

---

## ImageManager 设计

### 核心类

```python
from typing import List, Optional, Dict, Any
from dataclasses import dataclass


@dataclass
class ImageInfo:
    """图片信息"""
    image_id: int
    image_url: str
    image_type: str
    description: Optional[str]
    display_order: int
    metadata: Optional[ImageMetadata]


class ImageManager:
    """图片管理器"""

    def __init__(self, db, storage: ImageStorageAdapter):
        self.db = db
        self.storage = storage

    # ========== 查询方法 ==========

    def get_equipment_images(
        self,
        equipment_id: int,
        image_type: Optional[str] = None
    ) -> List[ImageInfo]:
        """获取装备的图片列表"""
        query = """
            SELECT image_id, image_url, image_type, description, display_order
            FROM product_images
            WHERE equipment_id = ?
        """
        params = [equipment_id]

        if image_type:
            query += " AND image_type = ?"
            params.append(image_type)

        query += " ORDER BY display_order, image_id"

        rows = self.db.execute(query, tuple(params))
        return [self._row_to_image_info(row) for row in rows]

    def get_rig_images(
        self,
        rig_type_id: int,
        image_type: Optional[str] = None
    ) -> List[ImageInfo]:
        """获取钓组的图片列表"""
        query = """
            SELECT image_id, image_url, image_type, description, display_order
            FROM product_images
            WHERE rig_type_id = ?
        """
        params = [rig_type_id]

        if image_type:
            query += " AND image_type = ?"
            params.append(image_type)

        query += " ORDER BY display_order, image_id"

        rows = self.db.execute(query, tuple(params))
        return [self._row_to_image_info(row) for row in rows]

    def get_fish_images(
        self,
        fish_species_id: int,
        image_type: Optional[str] = None
    ) -> List[ImageInfo]:
        """获取鱼类的图片列表"""
        query = """
            SELECT image_id, image_url, image_type, description, display_order
            FROM product_images
            WHERE fish_species_id = ?
        """
        params = [fish_species_id]

        if image_type:
            query += " AND image_type = ?"
            params.append(image_type)

        query += " ORDER BY display_order, image_id"

        rows = self.db.execute(query, tuple(params))
        return [self._row_to_image_info(row) for row in rows]

    def get_main_image(self, equipment_id: int) -> Optional[str]:
        """获取装备主图URL"""
        images = self.get_equipment_images(equipment_id, image_type='main')
        return images[0].image_url if images else None

    # ========== 写入方法 ==========

    def add_image(
        self,
        image_data: bytes,
        relative_path: str,
        image_type: str,
        equipment_id: Optional[int] = None,
        rig_type_id: Optional[int] = None,
        fish_species_id: Optional[int] = None,
        description: Optional[str] = None,
        display_order: int = 0
    ) -> int:
        """添加图片"""
        # 保存文件
        image_url = self.storage.save(image_data, relative_path)

        # 获取元数据
        metadata = self.storage.get_metadata(relative_path)

        # 写入数据库
        query = """
            INSERT INTO product_images (
                equipment_id, rig_type_id, fish_species_id,
                image_url, image_type, description, display_order,
                file_size, width, height, format
            ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
        """
        params = (
            equipment_id, rig_type_id, fish_species_id,
            image_url, image_type, description, display_order,
            metadata.file_size if metadata else None,
            metadata.width if metadata else None,
            metadata.height if metadata else None,
            metadata.format if metadata else None
        )

        return self.db.execute_write(query, params)

    def delete_image(self, image_id: int) -> bool:
        """删除图片（含物理文件和数据库记录）"""
        # 查询图片信息
        query = "SELECT image_url, embedding_id FROM product_images WHERE image_id = ?"
        rows = self.db.execute(query, (image_id,))

        if not rows:
            return False

        image_url = rows[0]['image_url']
        embedding_id = rows[0]['embedding_id']

        # 1. 删除物理文件
        relative_path = self._url_to_relative_path(image_url)
        self.storage.delete(relative_path)

        # 2. 删除向量（如果存在）
        if embedding_id:
            # TODO: 调用向量库删除
            pass

        # 3. 删除数据库记录
        delete_query = "DELETE FROM product_images WHERE image_id = ?"
        self.db.execute_write(delete_query, (image_id,))

        return True

    # ========== 向量化相关 ==========

    def mark_vectorized(self, image_id: int, embedding_id: str) -> None:
        """标记图片已向量化"""
        query = """
            UPDATE product_images
            SET embedding_id = ?, is_vectorized = 1, updated_at = CURRENT_TIMESTAMP
            WHERE image_id = ?
        """
        self.db.execute_write(query, (embedding_id, image_id))

    def get_unvectorized_images(self, limit: int = 100) -> List[Dict[str, Any]]:
        """获取未向量化的图片列表"""
        query = """
            SELECT image_id, image_url, description
            FROM product_images
            WHERE is_vectorized = 0
            LIMIT ?
        """
        return self.db.execute(query, (limit,))

    # ========== 私有方法 ==========

    def _row_to_image_info(self, row: Dict) -> ImageInfo:
        return ImageInfo(
            image_id=row['image_id'],
            image_url=row['image_url'],
            image_type=row['image_type'],
            description=row.get('description'),
            display_order=row.get('display_order', 0),
            metadata=None  # 需要时懒加载
        )

    def _url_to_relative_path(self, image_url: str) -> str:
        """从URL提取相对路径"""
        if image_url.startswith('file://'):
            return image_url.replace(f'file://{self.storage.base_path}/', '')
        return image_url
```

---

## 使用示例

### 添加产品图片

```python
# 初始化
storage = LocalImageStorage("src/tools/lure/data/images")
image_manager = ImageManager(db, storage)

# 读取图片文件
with open("shimano_zodias.jpg", "rb") as f:
    image_data = f.read()

# 添加主图
image_manager.add_image(
    image_data=image_data,
    relative_path="products/rods/shimano_zodias_264ml_main.jpg",
    image_type="main",
    equipment_id=1,
    description="禧玛诺ZODIAS 264ML鱼竿主图"
)
```

### 在格式化输出中使用图片

```python
def format_recommendation(results: List[Dict], user_specs: Dict) -> str:
    """格式化推荐报告（含图片）"""
    output = "# 路亚装备推荐报告\n\n"

    for i, item in enumerate(results, 1):
        equipment_id = item['equipment_id']
        main_image = image_manager.get_main_image(equipment_id)

        output += f"### {i}. {item['name']}（匹配度：{item['score']}%）\n\n"

        if main_image:
            output += f"![{item['name']}]({main_image})\n\n"

        output += f"- **价格**：¥{item['price']}\n"
        output += f"- **规格**：{item['specs']}\n\n"

    return output
```

### 获取钓组图解

```python
def format_rig_guide(rig_type_id: int) -> str:
    """格式化钓组指南（含图解）"""
    # 获取钓组信息
    rig_info = get_rig_info(rig_type_id)

    # 获取图解图片
    images = image_manager.get_rig_images(rig_type_id, image_type='infographic')

    output = f"# {rig_info['name']}组装指南\n\n"

    if images:
        output += "## 组装图解\n\n"
        for img in images:
            output += f"![{img.description or '组装图解'}]({img.image_url})\n\n"

    output += "## 组装步骤\n\n"
    output += rig_info['assembly_steps']

    return output
```

---

## 数据同步策略

### 图片删除时的级联处理

```python
def delete_equipment_with_images(equipment_id: int):
    """删除装备及其所有图片"""
    # 1. 获取所有关联图片
    images = image_manager.get_equipment_images(equipment_id)

    # 2. 逐个删除图片（含物理文件和向量）
    for img in images:
        image_manager.delete_image(img.image_id)

    # 3. 删除装备记录
    db.execute_write("DELETE FROM equipment WHERE equipment_id = ?", (equipment_id,))
```

### 向量化批处理

```python
def batch_vectorize_images():
    """批量向量化未处理的图片"""
    unvectorized = image_manager.get_unvectorized_images(limit=100)

    for img in unvectorized:
        # 加载图片
        image_data = load_image(img['image_url'])

        # 生成向量
        embedding = clip_model.encode_image(image_data)

        # 存入向量库
        embedding_id = chroma.add(
            embeddings=[embedding],
            metadatas=[{"image_id": img['image_id'], "description": img['description']}],
            ids=[f"img_{img['image_id']}"]
        )

        # 更新标记
        image_manager.mark_vectorized(img['image_id'], embedding_id)
```

---

## 相关文档

- [01-架构设计.md](./01-架构设计.md) - 整体架构设计
- [02-数据库设计.md](./02-数据库设计.md) - 数据库总览
- [07-向量知识库设计.md](./07-向量知识库设计.md) - 向量库设计（图片向量化）

---

**版本历史**：
- v1.0 (2025-11-24): 初始设计
