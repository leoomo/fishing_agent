# 02 - 代码实现

> **文档版本**: v4.0
> **最后更新**: 2025-11-21
> **状态**: ✅ 已完成

## 📑 导航

- [← 返回数据库设计总览](../02-数据库设计.md)
- [鱼竿表设计 →](./02-01-鱼竿表设计.md)
- [导环表设计 →](./02-05-导环表设计.md)

---

## 📚 代码位置

完整的Python实现代码位于备份文档中：
- **备份文档**：`database/02-数据库设计-v3.1-完整版-备份.md`（第335-585行）

本文档提供核心代码结构概览和快速参考。

---

## 🏗️ 核心类结构

```python
# src/tools/lure/database.py

import sqlite3
from pathlib import Path
from typing import Optional, List, Dict, Any
import threading
import json

class ProductionSQLiteDB:
    """生产级SQLite数据库（v4.0模块化版）"""

    _instance = None
    _lock = threading.Lock()

    def __new__(cls, *args, **kwargs):
        """单例模式"""
        if cls._instance is None:
            with cls._lock:
                if cls._instance is None:
                    cls._instance = super().__new__(cls)
        return cls._instance

    def __init__(self, db_path: str = None):
        """初始化数据库"""
        if hasattr(self, '_initialized'):
            return

        self.db_path = db_path or Path(__file__).parent / "data" / "equipment.db"
        self._local = threading.local()  # 线程本地连接
        self._init_database()
        self._initialized = True
```

---

## 🔌 连接管理

### 创建连接

```python
def _create_connection(self) -> sqlite3.Connection:
    """创建数据库连接"""
    conn = sqlite3.connect(
        self.db_path,
        timeout=30.0,
        check_same_thread=False
    )
    conn.row_factory = sqlite3.Row  # 返回字典格式

    # WAL模式配置
    conn.execute("PRAGMA journal_mode=WAL")
    conn.execute("PRAGMA synchronous=NORMAL")
    conn.execute("PRAGMA cache_size=-64000")
    conn.execute("PRAGMA busy_timeout=30000")
    conn.execute("PRAGMA temp_store=MEMORY")
    conn.execute("PRAGMA foreign_keys=ON")

    return conn
```

### 线程本地连接

```python
def _get_connection(self) -> sqlite3.Connection:
    """获取线程本地连接"""
    if not hasattr(self._local, 'connection'):
        self._local.connection = self._create_connection()
    return self._local.connection
```

---

## 🗄️ 表初始化

### 核心表创建

详细的建表语句请参考备份文档第396-584行，包括：

1. **brands表**（第476-490行）
2. **equipment表**（第434-457行）
3. **rod_specs表**（第459-494行）
4. **equipment_types表**（第492-510行）
5. **guide_specs表**（第512-536行）
6. **rod_guide_configurations表**（第538-560行）
7. **rod_guide_kits表**（第562-580行）
8. **rod_guide_kit_items表**（第582-600行）
9. **user_feedback表**（自行添加）

---

## 📊 查询方法

### 1. 鱼竿查询

参考 [02-01-鱼竿表设计.md](./02-01-鱼竿表设计.md) 中的查询示例。

核心方法：
```python
def search_rods(
    self,
    fishing_type: Optional[str] = None,
    power: Optional[str] = None,
    length_min: Optional[float] = None,
    length_max: Optional[float] = None,
    water_type: Optional[str] = None,
    price_max: Optional[float] = None,
    limit: int = 20
) -> List[Dict[str, Any]]:
    """查询鱼竿（详见鱼竿表设计文档）"""
    pass
```

### 2. 导环查询

参考 [02-05-导环表设计.md](./02-05-导环表设计.md) 中的查询示例。

核心方法：
```python
def search_guides(
    self,
    guide_type: Optional[str] = None,
    ring_material: Optional[str] = None,
    size_min: Optional[float] = None,
    size_max: Optional[float] = None,
    limit: int = 20
) -> List[Dict[str, Any]]:
    """查询导环（详见导环表设计文档）"""
    pass
```

### 3. 通用装备查询

```python
def search_equipment(
    self,
    category: str,
    price_max: Optional[float] = None,
    limit: int = 20
) -> List[Dict[str, Any]]:
    """查询装备（基础查询）"""
    conn = self._get_connection()
    cursor = conn.cursor()

    query = '''
        SELECT e.*, b.reputation_score, b.tier
        FROM equipment e
        LEFT JOIN brands b ON e.brand = b.brand_name
        WHERE e.category = ?
    '''
    params = [category]

    if price_max:
        query += ' AND e.price_min <= ?'
        params.append(price_max)

    query += ' ORDER BY b.reputation_score DESC, e.confidence DESC LIMIT ?'
    params.append(limit)

    cursor.execute(query, params)

    results = []
    for row in cursor.fetchall():
        equipment = dict(row)
        if equipment.get('specs_json'):
            equipment['specs'] = json.loads(equipment['specs_json'])
            del equipment['specs_json']
        results.append(equipment)

    return results
```

---

## 🔧 CRUD操作

### 添加装备

```python
def add_equipment(self, equipment_data: Dict[str, Any]) -> str:
    """添加装备"""
    conn = self._get_connection()
    cursor = conn.cursor()

    try:
        cursor.execute('''
            INSERT INTO equipment (id, name, brand, category, price_min, price_max,
                                 specs_json, data_source, confidence)
            VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)
        ''', (
            equipment_data['id'],
            equipment_data['name'],
            equipment_data['brand'],
            equipment_data['category'],
            equipment_data.get('price_min'),
            equipment_data.get('price_max'),
            json.dumps(equipment_data.get('specs', {}), ensure_ascii=False),
            equipment_data.get('data_source', 'manual'),
            equipment_data.get('confidence', 1.0)
        ))
        conn.commit()
        return equipment_data['id']
    except Exception as e:
        conn.rollback()
        raise
```

### 添加鱼竿规格

```python
def add_rod_specs(self, rod_data: Dict[str, Any]) -> None:
    """添加鱼竿规格"""
    conn = self._get_connection()
    cursor = conn.cursor()

    try:
        cursor.execute('''
            INSERT INTO rod_specs (
                equipment_id, fishing_type, water_type, target_species,
                length, power, action, weight,
                lure_weight_raw, lure_weight_min_g, lure_weight_max_g,
                carbon_grade, base_material, carbon_content, material_description,
                collapsed_length, sections_count, handle_type, handle_length,
                construction_type, max_drag
            ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
        ''', (
            rod_data['equipment_id'],
            rod_data['fishing_type'],
            rod_data.get('water_type'),
            json.dumps(rod_data.get('target_species', []), ensure_ascii=False),
            rod_data['length'],
            rod_data.get('power'),
            rod_data.get('action'),
            rod_data.get('weight'),
            rod_data.get('lure_weight_raw'),
            rod_data.get('lure_weight_min_g'),
            rod_data.get('lure_weight_max_g'),
            rod_data.get('carbon_grade'),
            rod_data.get('base_material'),
            rod_data.get('carbon_content'),
            rod_data.get('material_description'),
            rod_data.get('collapsed_length'),
            rod_data.get('sections_count'),
            rod_data.get('handle_type'),
            rod_data.get('handle_length'),
            rod_data.get('construction_type'),
            rod_data.get('max_drag')
        ))
        conn.commit()
    except Exception as e:
        conn.rollback()
        raise
```

---

## 💾 数据维护

### 更新装备

```python
def update_equipment(self, equipment_id: str, updates: Dict):
    """更新装备数据"""
    with self.transaction() as conn:
        cursor = conn.cursor()

        set_clause = ', '.join([f"{k} = ?" for k in updates.keys()])
        values = list(updates.values()) + [equipment_id]

        cursor.execute(f'''
            UPDATE equipment
            SET {set_clause}, updated_at = CURRENT_TIMESTAMP
            WHERE id = ?
        ''', values)
```

### 备份数据库

```python
def backup_database(self, backup_path: str):
    """备份数据库"""
    import shutil

    # WAL模式需要checkpoint
    conn = self._get_connection()
    conn.execute("PRAGMA wal_checkpoint(FULL)")

    # 复制文件
    shutil.copy2(self.db_path, backup_path)
```

---

## 🎯 使用示例

### 示例1：初始化并添加鱼竿

```python
# 初始化数据库
db = ProductionSQLiteDB()

# 添加品牌
conn = db._get_connection()
cursor = conn.cursor()
cursor.execute('''
    INSERT OR IGNORE INTO brands (brand_name, reputation_score, country, tier, is_premium)
    VALUES (?, ?, ?, ?, ?)
''', ('禧玛诺', 95.0, '日本', '一线', 1))
conn.commit()

# 添加鱼竿装备
rod_equipment = {
    'id': 'shimano_poison_264mh',
    'name': '禧玛诺毒牙21代 264MH',
    'brand': '禧玛诺',
    'category': '鱼竿',
    'price_min': 650.0,
    'price_max': 750.0,
    'specs': {'notes': '限量版涂装', 'warranty_years': 2},
    'data_source': 'manual',
    'confidence': 1.0
}
db.add_equipment(rod_equipment)

# 添加鱼竿规格
rod_specs = {
    'equipment_id': 'shimano_poison_264mh',
    'fishing_type': '路亚',
    'water_type': '淡水',
    'target_species': ['黑鱼', '鲈鱼', '翘嘴'],
    'length': 2.64,
    'power': 'MH',
    'action': 'F',
    'weight': 105,
    'lure_weight_raw': '7-21g',
    'lure_weight_min_g': 7.0,
    'lure_weight_max_g': 21.0,
    'carbon_grade': '36T',
    'base_material': '碳素',
    'carbon_content': 95.0,
    'material_description': '36T高模量碳素',
    'sections_count': 2,
    'handle_type': 'EVA',
    'construction_type': '并继'
}
db.add_rod_specs(rod_specs)
```

### 示例2：查询鱼竿

```python
# 查询路亚淡水竿，MH硬度，2.4-2.7米
results = db.search_rods(
    fishing_type='路亚',
    water_type='淡水',
    power='MH',
    length_min=2.4,
    length_max=2.7,
    price_max=800
)

for rod in results:
    print(f"- {rod['name']}: {rod['length']}m {rod['power']} {rod['action']}")
```

---

## 📦 完整代码参考

详细的完整代码实现请参考：
- **备份文档**：`database/02-数据库设计-v3.1-完整版-备份.md`（第335-850行）

包含：
- 完整的`ProductionSQLiteDB`类实现
- 所有表的初始化代码
- 事务管理和重试机制
- 完整的查询方法集合
- 数据维护工具方法

---

## 🔗 相关文档

- [← 数据库设计总览](../02-数据库设计.md)
- [鱼竿表设计](./02-01-鱼竿表设计.md)
- [导环表设计](./02-05-导环表设计.md)

---

## 📋 版本历史

- **v4.0 (2025-11-21)**: 从总览文档拆分出独立模块（简化版）
- **v3.1 (2025-11-21)**: 完整实现所有表和查询方法
